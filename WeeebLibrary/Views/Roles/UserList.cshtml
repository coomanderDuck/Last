@model IEnumerable<WeeebLibrary.Database.Entitys.User>

<h2>Список пользователей</h2>
<table class="lead">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-userid="@user.Id">Права доступа</a>
            </td>
        </tr>
    }
</table>